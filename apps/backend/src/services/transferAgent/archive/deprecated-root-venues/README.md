# 已弃用：根目录会场杂项归档

此处为从仓库根迁移前的 `acl/`、`neurips/`、`icml/`、`cvpr/` 完整拷贝。会场规则正文已统一到 `apps/backend/src/services/transferAgent/rules/*.md`；LaTeX 模板以 `templates/` 为准。

本地验证通过后，可删除整个 `deprecated-root-venues` 目录。
